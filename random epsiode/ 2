  
import os 
import random 

path='enter the path of your folder of videos in your pc"

#os.chdir is used to open the dir of given path 
os.chdir(path)

#folder_name is the folder which contains all of our videos 
#random.choicw will choose random videos from the folder
#os.listdir will list all the items are present your specified path 
folder_name=random.chioce(os.listdir(path)

#if your videos folder inside another folder#eg folder of movies contain 2 folders i.e folder1,folder2 and your want to choose folder1 
folder_path=str(os.path.realpath(folder_name)
os.chdir(folder_path)
file_name=random.chioce(os.listdir(folder_path)

print("enjoy")
#for mac use "open" instead of "start"
os.system("start"+folder_name)
